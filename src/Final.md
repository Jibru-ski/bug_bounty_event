## Challenge Completion
### Bugs found & solution 

On line [9]
changed the sign from - to + 
   ```a + b``` ~ It was logic error

On line [16]
added ``` + 1 ``` ~ It should run upto 1 

On line [22]
added ``` variable r ``` ~ Fixed missing variable

On line [31]
added ```show_secret``` ~ Called the missing function. 